spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/test;
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true
#    database-platform: org.hibernate.dialect.h2.H2Dialect
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace